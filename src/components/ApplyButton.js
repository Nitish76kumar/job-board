import React from "react";

const ApplyButton = () => (
  <button className="w-full mt-4 bg-teal-500 text-white py-2 rounded hover:bg-teal-600 transition-colors flex items-center justify-center gap-2">
    Apply
    <span>➜</span>
  </button>
);

export default ApplyButton;
